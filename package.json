{
  "name": "taiga",
  "version": "1.0.0",
  "description": "taiga",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd client && npm i && npm run build && cd ../server && npm i && tsc && node dist/bin/www"
  },
  "author": "",
  "license": "ISC"
}
